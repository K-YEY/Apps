import 'package:flutter/material.dart';

class RattingTabScreen extends StatelessWidget {
  const RattingTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ratting'),
    );
  }
}
